# Database - Production will use managed PostgreSQL
DATABASE_URL="postgresql://pontohub:pontohub123@localhost:5432/pontohub_production?schema=public"

# JWT
JWT_SECRET="pontohub-super-secret-jwt-key-2024-production"
JWT_EXPIRES_IN="7d"
JWT_REFRESH_SECRET="pontohub-super-secret-refresh-key-2024-production"
JWT_REFRESH_EXPIRES_IN="30d"

# Server
PORT=3001
NODE_ENV="production"

# Email Configuration
SMTP_HOST="smtp.gmail.com"
SMTP_PORT=587
SMTP_USER="noreply@pontohub.com"
SMTP_PASS="app-password-here"
FROM_EMAIL="noreply@pontohub.com"
FROM_NAME="PontoHub Portal"

# File Upload
MAX_FILE_SIZE=5242880
UPLOAD_PATH="./uploads"

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# CORS - Will be updated with production frontend URL
FRONTEND_URL="*"

